// Estrutura base para conexão futura com API do banco Efi

export function criarTransacao(valor, tipo) {
  // Simulação por enquanto
  console.log(`[MOCK] Transação ${tipo} de R$${valor.toFixed(2)} enviada para Efi.`);
}